import { Routes } from '@angular/router';
import { UserProfileComponent } from '../assets/views/user-profile/user-profile.component';

export const routes: Routes = [
    {path: 'user-profile', component: UserProfileComponent},
];
